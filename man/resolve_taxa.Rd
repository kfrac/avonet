% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{resolve_taxa}
\alias{resolve_taxa}
\title{Resolve a taxon name to the species it contains}
\usage{
resolve_taxa(taxon, rank = NULL, taxonomy)
}
\arguments{
\item{taxon}{Character(1) taxon name, e.g. \code{"Buteo buteo"}, \code{"Buteo"} or
\code{"Accipitridae"}. Surrounding whitespace is trimmed.}

\item{rank}{Character(1) or \code{NULL}. One of \code{"species"}, \code{"genus"},
\code{"family"} or \code{"order"}, matched case-insensitively. \code{NULL} (the default)
auto-detects the rank.}

\item{taxonomy}{Integer taxonomy ID matched against \code{species_tax}, the same
value passed to \code{\link[=get_traits]{get_traits()}}.}
}
\value{
Character vector of \code{species_name} values, of length 1 or more.
Errors if \code{rank} is not one of the four valid ranks, or if the taxon
matches no species under the requested taxonomy.
}
\description{
Internal helper behind \code{\link[=get_traits]{get_traits()}}. Expands a name at any supported rank
into the vector of species names it covers, restricted to one taxonomy
version.
}
\details{
When \code{rank} is \code{NULL} it is auto-detected with \code{\link[=detect_rank]{detect_rank()}}, and the
detected rank is reported via a message so the caller can see how an
ambiguous name was read.

Genus has no dedicated column in the \code{species} table, so a genus is matched
with \verb{LIKE 'Genus \%'} against \code{species_name}. The other ranks match
\code{species_name}, \code{species_family} or \code{species_order} exactly. Results are
deduplicated and returned in alphabetical order.
}
\examples{
\dontrun{
# assumes an open connection, see connect_db()
avonet:::resolve_taxa("Buteo", taxonomy = 1)
avonet:::resolve_taxa("Accipitridae", rank = "family", taxonomy = 1)
}
}
\seealso{
\code{\link[=detect_rank]{detect_rank()}} for the auto-detection step; \code{\link[=get_traits]{get_traits()}}, which
calls this once per supplied name.
}
\keyword{internal}
