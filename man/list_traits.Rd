% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/list_traits.R
\name{list_traits}
\alias{list_traits}
\title{Summarize the traits in one database table}
\usage{
list_traits(table_name)
}
\arguments{
\item{table_name}{Character(1). Name of a trait table in the AVONET
database, interpolated as a quoted identifier. \code{\link[=get_trait_list]{get_trait_list()}} calls
this with \code{"eco_trait_species"}, \code{"social_trait_species"},
\code{"morph_trait_specimen"} and \code{"geo_data_species"}. A name that is not a
table raises a Postgres error.}
}
\value{
A data frame with one row per trait and the columns:
\describe{
\item{\code{trait}}{Trait name, with its table prefix stripped.}
\item{\code{resolution}}{\code{"species"} or \code{"specimen"}, i.e. whether the trait
is recorded once per species or once per measured individual.}
\item{\code{description}}{Free-text description from \code{\link[=get_trait_values]{get_trait_values()}},
or \code{NA} where none is recorded.}
\item{\code{value}}{\code{"numeric"}, or the observed categorical levels as a
comma-separated string.}
\item{\code{primary_source}}{Abbreviated citation for the publication most
of the trait's values come from; see Details.}
}
Traits with no attributable source are absent, so the result can be
shorter than the number of trait columns in the table.
}
\description{
Internal helper behind \code{\link[=get_trait_list]{get_trait_list()}}. Reads a single trait table and
returns one row per trait column, recording what the trait is, what values
it can take, whether it is measured per species or per specimen, and which
publication most of its values are attributed to.
}
\details{
Bookkeeping columns are excluded before summarizing: anything ending in
\verb{_id}, \verb{_src} or \verb{_source}, plus the record-level columns of
\code{morph_trait_specimen} (\code{measure_date}, \code{measurer_id}, \code{measurer_comment}
and friends). Remaining column names are stripped of their table prefix
(\code{ect_}, \code{spd_}, \code{geo_}, \code{rts_}, \code{sts_}) so they match the names users see
in \code{\link[=get_traits]{get_traits()}} output.

Descriptions are joined on trait name from \code{\link[=get_trait_values]{get_trait_values()}}; a trait
with no matching entry there keeps a \code{NA} description rather than being
dropped.
\subsection{Possible values}{

The \code{value} column summarizes what a trait can contain: the literal string
\code{"numeric"} for numeric columns, or a comma-separated, alphabetically
sorted list of the distinct values actually observed for categorical ones.
This is derived from the data rather than from the Postgres enum
definition, so a level that exists in the schema but is never used will not
appear.
}

\subsection{Where \code{primary_source} comes from}{

The database records sources differently depending on the table, so this is
derived in two ways:
\itemize{
\item \code{morph_trait_specimen} has no per-trait or per-measurement source column
— its \code{source_id} and \code{lit_id} are record-level, not tied to an
individual measurement — so every morphological trait is attributed to
\code{"Tobias et al. (2022)"}. The same applies to \code{mass_value}, which is
queried separately from \code{mass_species} but reported from that source for
the same reason.
\item Every other table pairs each trait with its own source column via
\code{derive_source_map()}, then queries that column for its most frequently
cited \code{trait_src_id} and resolves it to a \code{lit_abbrev} from
\code{trait_source_detailed}. Where several sources tie for most cited, all of
them are kept and comma-joined. A source column that is \code{NULL} throughout
yields \code{NA}.
}
}

\subsection{Dropped rows}{

Traits whose \code{primary_source} resolves to \code{NA}, or to \code{"AVOTRAITS"} alone,
are removed from the result on the grounds that a trait with no
attributable external source is not citable.
}
}
\examples{
\dontrun{
db_user <- keyring::key_list("avonet")[1, "username"]
db_password <- keyring::key_get("avonet", username = db_user)
connect_db(username = db_user, pw = db_password)

# Species-level ecological traits, sources derived from the data
avonet:::list_traits("eco_trait_species")

# Specimen-level morphology, all attributed to Tobias et al. (2022)
avonet:::list_traits("morph_trait_specimen")

disconnect_db()
}
}
\seealso{
\code{\link[=get_trait_list]{get_trait_list()}}, the exported wrapper that calls this once per
trait group; \code{\link[=get_trait_values]{get_trait_values()}} for the underlying trait descriptions;
\code{\link[=get_trait_levels]{get_trait_levels()}} for the valid levels of a single categorical trait.
}
\keyword{internal}
