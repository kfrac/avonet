% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{detect_rank}
\alias{detect_rank}
\title{Infer the taxonomic rank of a name}
\usage{
detect_rank(taxon)
}
\arguments{
\item{taxon}{Character(1) taxon name to look up. Surrounding whitespace is
trimmed.}
}
\value{
Character(1): one of \code{"species"}, \code{"genus"}, \code{"family"} or
\code{"order"}. Errors if nothing matches, prompting the caller to supply
\code{rank} explicitly or check the spelling.
}
\description{
Internal helper behind \code{\link[=resolve_taxa]{resolve_taxa()}}. Probes the \code{species} table to work
out whether a supplied name is a species binomial, a genus, a family or an
order.
}
\details{
Detection uses the shape of the name first, then a lookup:
\itemize{
\item \code{"species"} — the name contains a space, i.e. is a \verb{Genus species}
binomial, and matches \code{species_name} exactly.
\item \code{"genus"} — a single word, and \verb{species_name LIKE 'Word \%'} returns a
hit, i.e. the name is the first word of at least one binomial.
\item \code{"family"} — a single word matching \code{species_family} exactly.
\item \code{"order"} — a single word matching \code{species_order} exactly.
}

Single-word names are tried in that order and the first hit wins, so a name
that is both a genus and a family resolves as a genus. Each check is a
separate \verb{LIMIT 1} query, so detection costs up to three round trips.
}
\examples{
\dontrun{
# assumes an open connection, see connect_db()
avonet:::detect_rank("Buteo buteo")   # "species"
avonet:::detect_rank("Buteo")         # "genus"
avonet:::detect_rank("Accipitridae")  # "family"
}
}
\seealso{
\code{\link[=resolve_taxa]{resolve_taxa()}}, which calls this whenever its \code{rank} argument is
\code{NULL}.
}
\keyword{internal}
