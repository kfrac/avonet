% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{tidy_species_columns}
\alias{tidy_species_columns}
\title{Harmonize species-level column names}
\usage{
tidy_species_columns(df, source_cols = FALSE)
}
\arguments{
\item{df}{Data frame of raw \code{query_species_traits()} output.}

\item{source_cols}{Logical. Keep the \verb{_src} / \verb{_source} columns inline?
Defaults to \code{FALSE}. \code{TRUE} also retains the \code{id} column, because step 3
removes both under the same suffix rule.}
}
\value{
The data frame with cleaned-up column names: 17 columns when
\code{source_cols = FALSE}, 26 when \code{TRUE}.
}
\description{
Shared column clean-up for species-level query output, so that
\code{\link[=get_species]{get_species()}} and the \code{data} element of \code{\link[=get_traits]{get_traits()}} always return the
same set of columns. Kept in one place because the two entry points
otherwise drift apart silently.
}
\details{
Three steps, in order:
\enumerate{
\item Strip the table prefixes (\code{ect_}, \code{spd_}, \code{geo_}, \code{species_}) that exist
only to keep column names unique across the joined tables.
\item Rename the resulting \code{name} column to \code{species}.
\item Unless \code{source_cols = TRUE}, drop the bookkeeping columns: every
per-trait \verb{_src} / \verb{_source} column, plus \code{species_id}, which matches the
\code{"id"} suffix once its prefix has been stripped.
}

Note that \code{mass_value} and \code{mass_flag} keep their prefix, since \code{mass_} is
not in the strip list — the column is named for the trait itself rather than
for its table.
}
\examples{
\dontrun{
# assumes an open connection, see connect_db()
raw <- avonet:::query_species_traits(species = "Buteo buteo", taxonomy = 1)

names(avonet:::tidy_species_columns(raw))
names(avonet:::tidy_species_columns(raw, source_cols = TRUE))
}
}
\seealso{
\code{\link[=get_species]{get_species()}} and \code{\link[=get_traits]{get_traits()}}, the two callers.
}
\keyword{internal}
