% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/helper_functions.R
\name{derive_source_map}
\alias{derive_source_map}
\title{Map trait columns to their literature-source columns}
\usage{
derive_source_map(table_name, raw_cols)
}
\arguments{
\item{table_name}{Character(1) table name. Used only to make the error
message specific; no query is issued against it here.}

\item{raw_cols}{Character vector of every column name in the table, as
returned by \verb{SELECT *}, before any prefix or suffix stripping.}
}
\value{
A named character vector: names are the trait columns, values are
the source columns they resolve to.
}
\description{
Internal helper behind \code{\link[=list_traits]{list_traits()}}. Works out which \verb{_src} / \verb{_source}
column holds the literature reference for each trait column in a table.
}
\details{
Most trait columns have a dedicated source column formed by suffixing
\verb{_src} or \verb{_source} onto the trait column's own name, e.g. \code{ect_habitat}
pairs with \code{ect_habitat_src}.

Trait columns with no such match fall back to a single shared "leftover"
source column, provided exactly one is available — this is how the spatial
columns of \code{geo_data_species} all resolve to \code{spd_spatial_source}. If that
fallback would be ambiguous, meaning zero or several leftover source
columns remain for the unmatched traits, the function raises an error of
class \code{avonet_error_unresolved_source} rather than guessing.
}
\examples{
\dontrun{
cols <- c("species_id", "spd_min_latitude", "spd_range_size",
          "spd_spatial_source")

# all three spatial traits share the one leftover source column
avonet:::derive_source_map("geo_data_species", cols)
}
}
\seealso{
\code{\link[=list_traits]{list_traits()}}, the only caller.
}
\keyword{internal}
